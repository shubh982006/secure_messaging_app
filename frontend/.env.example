# Point the browser at the FastAPI backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional: defaults to NEXT_PUBLIC_API_URL with http(s) swapped for ws(s).
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
